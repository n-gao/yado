services:
  yado:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - yado-data:/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/yado.db
    restart: unless-stopped

volumes:
  yado-data:
